#lang sicp

(define (append list1 list2)
  (if (null? list1)
      list2
      (cons (car list1) (append (cdr list1) list2))))

(define (reverse items)
  (if (null? items)  ; 检查列表本身是否为空,而不是cdr
      nil
      (append (reverse (cdr items)) (list (car items)))))

(reverse (list 1 4 9 16 25))